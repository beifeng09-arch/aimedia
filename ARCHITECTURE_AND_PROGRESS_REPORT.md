# OpenClaw 媒体智能体团队系统

## 架构与项目进度深度报告

更新时间：2026-03-23  
项目目录：`/Users/heyu/Documents/Projects/AI Media`

---

## 1. 报告目的

本报告用于汇总当前项目的产品目标、系统架构、模块职责、运行方式、已完成能力、当前限制、风险点与下一阶段建议，方便进行一次阶段性审阅。

这份报告重点回答 5 个问题：

1. 这个系统现在要解决什么问题
2. 当前产品形态与理想形态之间差多远
3. 已经落地了哪些核心能力
4. 系统现在的技术结构是否支撑后续扩展
5. 下一步最值得投入的工作是什么

---

## 2. 产品目标

### 2.1 长期目标

建设一套基于 OpenClaw、Codex、多智能体协作与后续工具链接入的媒体生产团队系统，使用户可以通过微信或时间调度发起任务，并由系统自动完成：

- 节目选题
- 研究与资料整理
- 口播脚本与长短视频提纲
- 分镜与剪辑计划
- 数字人口播适配
- 发布文案生成
- 定时执行与结果回传

最终理想状态不是“一个问答机器人”，而是一支可持续协作的“媒体内容团队”。

### 2.2 当前阶段目标

当前阶段为 `MVP / Phase 1.5`，目标是先把“可用闭环”做出来，而不是一步做到全自动生产级系统。

当前系统聚焦于：

- 两条可运行内容流程
  - 东南军情
  - 数字货币财经
- 本地 API 与 OpenClaw / 微信消息入口
- 审核节点暂停与恢复
- 调度器的初版时间驱动能力
- 工具层 mock/stub 以保证流程跑通

当前策略是“先跑通骨架，再逐步替换关键工具为真实能力”。

---

## 3. 产品形态定义

### 3.1 当前产品定位

当前产品更准确地说是：

`一个具备多智能体编排、消息驱动、审核控制和时间调度能力的半自动内容生产系统`

它已经不是单个 prompt，也不是单纯脚本集合，而是具备以下产品特征：

- 可以接任务
- 可以判断任务类型
- 可以分发给不同角色
- 可以形成结构化产物
- 可以在关键节点停住等待审核
- 可以从审核后恢复执行
- 可以按计划时间自动触发任务

### 3.2 理想产品形态

理想状态应该是一个“双引擎媒体团队系统”：

- 事件驱动：用户通过微信直接下发任务
- 时间驱动：系统按预设时间自动生成固定栏目

并且具备以下体验：

- 用户只需发一句话
- 团队自动开始执行
- 中途在必要时请求审核
- 到时给出结果
- 如有失败，给出原因和下一步建议

这意味着后续重点将从“有没有模块”转向“有没有稳定运营能力”。

---

## 4. 产品架构总览

### 4.1 总体分层

当前系统可以分为 8 层：

1. 输入层  
   CLI、HTTP API、OpenClaw 事件、微信统一消息入口、调度器触发

2. 路由层  
   对任务进行分类，分流到军情、财经、混合或系统流程

3. 编排层  
   由 orchestrator 管理任务初始化、流程调用、状态存储、结果输出

4. 智能体层  
   通过角色配置和 prompt，生成不同阶段的内容产物

5. 审核层  
   在 `plan` 与 `content_pack` 阶段执行暂停、审批与回退

6. 工具层  
   web_search、ffmpeg_edit、tts_generate 等统一适配接口

7. 调度层  
   基于时间规则自动触发预定义任务

8. 输出层  
   产物文件、状态快照、日志与 API 响应

### 4.2 当前核心流程图（文字版）

#### 事件驱动流程

用户消息  
-> OpenClaw / 微信入口  
-> `interfaces/api_server.py`  
-> `core/orchestrator.py`  
-> `core/router.py`  
-> 对应 workflow  
-> 多智能体产出  
-> 审核节点  
-> 输出文件与响应文本

#### 时间驱动流程

调度任务  
-> `scheduler/jobs.yaml`  
-> `scheduler/scheduler.py`  
-> `orchestrator.run_request(...)`  
-> workflow  
-> 结果写入产物与调度状态文件

---

## 5. 技术结构说明

### 5.1 目录结构

当前项目已形成清晰的职责边界：

- `core/`
  - 运行时核心
  - 路由、状态模型、编排、审核、日志

- `agents/`
  - 每个角色的 YAML 配置

- `prompts/`
  - 每个角色的 system prompt

- `workflows/`
  - 军情、财经、混合专题流程定义

- `tools/`
  - 外部工具统一适配层

- `interfaces/`
  - API、OpenClaw、微信消息协议与会话状态映射

- `integrations/`
  - 目前主要用于 OpenClaw 绑定

- `scheduler/`
  - 时间任务定义与调度引擎

- `templates/`
  - 节目模板定义

- `outputs/`
  - 实际任务产物、状态与调度状态

- `tests/`
  - 行为回归测试

### 5.2 核心代码组件

#### `main.py`

总入口，负责挂接所有 CLI 命令：

- `run`
- `resume`
- `review`
- `serve`
- `bind-openclaw`
- `schedule-list`
- `schedule-run-due`
- `schedule-run-job`

#### `core/orchestrator.py`

系统中最关键的“编排器”。

职责包括：

- 接任务
- 初始化 job 目录
- 调 workflow
- 管状态文件
- 写结果与日志
- 输出 job 快照

这是当前系统最像“总控后台”的部分。

#### `core/router.py`

负责把自然语言任务映射到：

- military
- finance
- mixed
- system

当前以规则优先，保留 LLM 分类器接口。

#### `core/agent_runner.py`

当前采用“配置 + 内建 handler”的方式模拟智能体执行。  
这是一个阶段性方案：它已经足以支撑架构验证，但未来如果接真实模型调用，这里会成为主要替换点之一。

#### `interfaces/api_server.py`

当前产品体验的关键模块。

已支持：

- `/team/call`
- `/jobs/<job_id>`
- `/jobs/<job_id>/review`
- `/jobs/<job_id>/resume`
- `/adapters/wechat/message`
- `/adapters/openclaw/task`
- `/adapters/openclaw/event`
- `/adapters/openclaw/weixin/event`

这意味着系统不再只是本地脚本，而是一个可被外部系统驱动的服务。

#### `scheduler/scheduler.py`

当前时间驱动能力的核心。

已支持：

- `daily`
- `weekly`
- 时区
- 单日去重
- 手动执行
- 到点执行

这是从“用户主动叫团队”走向“团队按时上班”的第一步。

---

## 6. 多智能体团队设计现状

### 6.1 已落地智能体

当前已配置并接入：

- `director_agent`
- `operator_agent`
- `military_editor_agent`
- `finance_editor_agent`
- `researcher_agent`
- `scriptwriter_agent`
- `storyboard_agent`
- `avatar_host_agent`
- `editor_agent`
- `publisher_agent`

### 6.2 当前智能体协作方式

当前并不是多个真实独立模型并发运行，而是：

- 用独立角色配置表达职责分工
- 用统一 `agent_runner` 调度不同角色输出
- 用 workflow 明确执行顺序

这是一种“结构先行”的实现方式。  
好处是：

- 架构清晰
- 扩展容易
- 成本可控
- 调试简单

不足是：

- 智能体之间目前还没有真实的上下文自治和自适应协作
- 还没有并行调度、冲突解决、动态重规划能力

换句话说，目前是“流程化团队骨架”，还不是“自治型团队组织”。

---

## 7. 当前支持的业务流程

### 7.1 东南军情流程

当前流程已具备：

- 任务分类
- 总导演计划
- 军情主编结构设计
- 研究 brief
- 脚本提纲
- 分镜提纲
- 剪辑计划
- 发布文案

可输出产物：

- `review_plan.md`
- `research_brief.md`
- `script_outline.md`
- `storyboard.md`
- `edit_plan.md`
- `publish_pack.md`

### 7.2 财经周报流程

当前流程已具备：

- 任务分类
- 总导演计划
- 财经主编结构设计
- 研究 brief
- 脚本提纲
- 数字人口播稿
- 剪辑计划
- 发布文案

可输出产物：

- `review_plan.md`
- `research_brief.md`
- `script_outline.md`
- `avatar_script.md`
- `edit_plan.md`
- `publish_pack.md`

### 7.3 混合专题流程

当前已存在 mixed workflow 骨架，但尚未发展为真正独立的高级专题产品线。  
它更像“为后续扩展保留接口”。

---

## 8. 消息驱动能力现状

### 8.1 已完成能力

微信 / OpenClaw 会话内当前支持：

- `做一期...`
- `状态`
- `通过`
- `修改：...`
- `拒绝`
- `继续`

这意味着会话已经具备“对话式控制团队”的最小闭环。

### 8.2 会话状态能力

系统已新增会话级最新任务映射：

- 每个会话都会记录最近一个 `job_id`
- 后续可以在不带 `job_id` 的情况下发状态与审批命令

这是一个很重要的产品体验改进，因为它让系统开始像“团队助手”，而不是“接口集合”。

### 8.3 OpenClaw 绑定状态

当前项目已实现：

- OpenClaw 微信会话绑定到当前项目线程
- 绑定状态写入 OpenClaw 插件状态文件
- 可通过 CLI 重新绑定

这解决了“消息进入哪个线程、对应哪个工作区”的关键问题。

---

## 9. 时间驱动能力现状

### 9.1 已完成能力

当前已经支持：

- 定义固定栏目任务
- 每天或每周按时间触发
- 同一天防重复执行
- 手动执行单条调度任务
- 查看调度任务状态

### 9.2 当前示例任务

当前默认已配置：

- `military_weekday_briefing`
  - 工作日 19:30
  - 任务：`做一期南海局势节目`

- `crypto_weekly_report`
  - 周日 20:00
  - 任务：`做一期比特币周报`

### 9.3 当前缺失

虽然调度器已经可用，但还没有形成完整的“定时交付系统”，主要缺口在：

- 没有常驻运行器
- 没有调度触发后的自动消息回传
- 没有超时与 SLA 通知
- 没有“开始执行 / 执行中 / 已完成”推送节奏

所以目前调度器是“引擎”，还不是“运营系统”。

---

## 10. 工具层现状

### 10.1 已有工具接口

已统一抽象为：

- `web_search`
- `video_download`
- `transcript`
- `subtitle_generate`
- `ffmpeg_edit`
- `tts_generate`
- `avatar_presenter`
- `publish_output`

### 10.2 当前状态

全部为 stub/mock。

好处：

- 流程可以完整跑通
- 不受外部 API 与工具环境阻塞
- 架构层已完成验证

不足：

- 研究内容仍然是 mock 线索，不是真实情报源
- 后期执行仍然是说明书，不是真实成片链路
- 发布动作还未真正触达平台

### 10.3 工具替换建议优先级

建议替换顺序：

1. `web_search`
2. `publish_output`
3. `transcript`
4. `ffmpeg_edit`
5. `tts_generate`
6. `avatar_presenter`

理由是：优先提升信息质量和交付质量，再提升后期自动化程度。

---

## 11. 当前测试与稳定性状态

### 11.1 测试覆盖

当前已有测试覆盖：

- 路由分类
- 军情流程
- 财经流程
- 审核暂停 / 恢复 / revise
- 工具统一返回结构
- API 服务
- 微信消息闭环
- OpenClaw 事件桥
- OpenClaw 绑定
- 调度器执行与防重复

### 11.2 当前测试状态

最近一次回归测试结果：

- `19 passed`

这说明当前项目已经不是概念草图，而是有一定可维护性和回归保障的工程。

### 11.3 当前稳定性判断

如果按“原型系统”的标准来看，当前稳定性已经不错。  
如果按“生产服务”的标准来看，还存在明显缺口：

- 还没有真正的异步队列
- 还没有任务并发控制
- 还没有 delivery 层可靠回执
- 还没有服务级健康恢复机制
- 还没有持久化数据库

---

## 12. 项目进度评估

### 12.1 已完成里程碑

#### 里程碑 A：骨架完成

已完成。

包括：

- 目录结构
- 配置文件
- 角色配置
- 模板定义
- 主入口

#### 里程碑 B：双流程跑通

已完成。

包括：

- 军情流程
- 财经流程
- 产物输出
- 审核暂停与恢复

#### 里程碑 C：消息闭环

已完成。

包括：

- API 服务
- 微信消息入口
- OpenClaw 事件桥
- 会话状态映射

#### 里程碑 D：线程绑定

已完成。

包括：

- OpenClaw 当前会话绑定到项目线程
- 可复用 CLI 重绑能力

#### 里程碑 E：时间调度初版

已完成。

包括：

- jobs 配置
- 调度执行
- 单日去重
- 手动触发

### 12.2 当前总体完成度判断

按“目标产品”的整体路线粗估：

- 架构完成度：`75%`
- 消息驱动完成度：`80%`
- 调度驱动完成度：`55%`
- 工具生产化完成度：`20%`
- 最终成片自动化完成度：`15%`
- 可运营化完成度：`35%`

综合看，当前已经完成的是：

`一个强架构、可运行、可审阅、可演进的 MVP 平台`

尚未完成的是：

`真正的生产级自动交付与运营化能力`

---

## 13. 当前主要问题与风险

### 13.1 产品层风险

- 用户体验还依赖本地服务常驻
- 调度结果不会自动回微信
- 当前审核仍偏工程化，不够“运营化”

### 13.2 技术层风险

- 没有数据库，主要依赖文件状态
- 没有异步任务队列
- 没有严格的并发与重入控制
- 没有系统级崩溃恢复机制

### 13.3 内容层风险

- 工具层仍是 mock，内容事实质量未达到生产要求
- 当前脚本与研究更多是结构验证，不是深度实战产出
- 发布前风险控制仍需人工把关

### 13.4 集成层风险

- OpenClaw 插件侧虽然已绑定，但还没有完成真正意义上的“官方双向稳定集成”
- 目前更像“项目服务已准备好对接”，而不是“渠道已产品化封装”

---

## 14. 下一阶段建议

### 14.1 第一优先级：常驻运行

目标：让系统不需要手工启动。

建议工作：

- 用 macOS `launchd` 挂 `main.py serve`
- 用 `launchd` 或守护方式定时跑 `schedule-run-due`
- 增加服务健康检查脚本

意义：

- 这是从“本地工具”走向“准服务”的第一步

### 14.2 第二优先级：消息回传层

目标：让系统主动给用户结果，而不是用户来查。

建议工作：

- 交付层 `delivery/`
- 执行开始通知
- 审核提醒通知
- 完成回传通知
- 超时通知

意义：

- 这是从“可运行”走向“像团队协作”的关键体验升级

### 14.3 第三优先级：真实工具替换

目标：提升内容质量与交付可信度。

建议顺序：

1. `web_search`
2. `publish_output`
3. `transcript`
4. `ffmpeg_edit`
5. `tts_generate`
6. `avatar_presenter`

意义：

- 这是从“结构验证”走向“可发布生产”的核心一步

### 14.4 第四优先级：任务系统升级

目标：让系统具备更强的运营能力。

建议工作：

- conversation -> jobs 多任务索引
- latest job + active job 区分
- `重跑`
- `取消`
- `延后`
- `改档`

意义：

- 这是从“工程流程”走向“实际运营后台”的必要演进

---

## 15. 审阅建议

建议你本轮重点审这 6 个问题：

1. 当前多智能体角色划分是否符合你对团队分工的期待
2. 当前军情 / 财经两条流程是否满足你想要的节目生产节奏
3. 审核节点放在 `plan` 与 `content_pack` 是否合理
4. 微信里的命令词是否够自然，是否需要更像真人团队
5. 调度任务的栏目设置是否符合你的实际运营节奏
6. 下一步优先做“常驻 + 回传”还是“真实工具接入”

---

## 16. 结论

当前项目已经成功跨过了最难的一道门槛：

`从概念设计进入了一个可运行、可审阅、可迭代的系统阶段。`

它现在已经具备：

- 架构正确性
- 流程完整性
- 消息入口
- 审核机制
- 时间驱动能力
- OpenClaw 线程绑定

它尚未具备：

- 生产级自动交付
- 稳定的主动结果回传
- 真实媒体工具链
- 可长期运营的服务化支撑

因此，当前最准确的阶段判断是：

`这已经是一套“团队操作系统的可运行 MVP”，下一步应从能力铺设转向运营化与交付化。`

